#!/bin/sh
set -e  # Exit immediately if any command fails

# Run lint-staged (handles desktop_app path internally)
pnpm lint-staged

# Run tests
echo "Running tests..."
pnpm test

# Run codegen
echo "Running codegen..."
pnpm codegen:all
